alias ll='ls -alF'
alias la='ls -A'

alias c='clear'
alias d="docker"
alias f="find . -name"
alias g='git'
alias y='yazi'

alias dokcer="docker"
alias dc="docker compose"

if [ $(uname) = "Darwin" ]; then
    alias ls='ls -G '
elif [ $(uname) = "Linux" ]; then
    alias ls='ls -F --color=always '
fi
